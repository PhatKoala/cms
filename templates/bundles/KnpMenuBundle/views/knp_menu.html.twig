{% extends 'knp_menu.html.twig' %}

{% block linkElement %}
    {% import _self as knp_menu %}
    <a href="{{ item.uri }}"{{ knp_menu.attributes(item.linkAttributes) }}>
        {% if item.extra('icon') is not none %}
            <i class="fa fa-{{ item.extra('icon') }} mr-2"></i>
        {% endif %}
        {{ block('label') }} !!!
    </a>
{% endblock %}